import { createTheme } from '@mui/material'

const theme = createTheme({
  palette: {
    primary: {
      main: '#9C51E0',
      light: '#B667F1',
      dark: '#8A39E1'
    },
    secondary: {
      main: '#ECC488'
    },
    background: {
      default: '#F7E8FD'
    }
  }
})

export default theme
