import { css } from '@emotion/react'

export default {
  root: css({
    height: '100vh',
    width: '100%',
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center'
  }),
  pageContainer: css({
    paddingInline: '30px',
    paddingTop: '30px',
    width: '100%'
  })
}
